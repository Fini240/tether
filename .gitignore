/target
**/*.rs.bk
.DS_Store

# Local machine identity + paired-peer state. Never commit: contains the
# private key this machine uses to authenticate to its peers.
/.tether-state/

# Release artifacts, rebuilt by packaging/macos/bundle.sh
/dist/
